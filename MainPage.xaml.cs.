using System;

namespace AppConsumo
{
    public class Program
    {
        public static void Main()
        {
            // Requisitos da Agenda 10
            string marcaVeiculo = "Toyota"; 
            string modeloVeiculo = "Corolla"; 

            // Sua lógica de cálculo
            double distancia = 150.0; 
            double consumoMedio = 12.0;
            double litros = distancia / consumoMedio;

            // Mensagem para o seu vídeo
            Console.WriteLine($"O cálculo de {litros:F2} litros está pronto para o seu {marcaVeiculo} {modeloVeiculo}.");
        }
    }
}
